# Project Overview

This repository is a shell-based CLI for bootstrapping, installing, and syncing AI configuration files.

Focus:
- install a `simply` binary to `~/.local/bin`
- initialize the `.ai/` project structure
- sync `.ai` content to tool-specific instruction outputs
- keep behavior stable across macOS and Linux

Do not modify generated sync outputs directly. `.ai/` is the source of truth.
